#include<stdio.h>
int main(void)
{
	int no,i,x,y,d;
	printf("enter the number:");
	scanf("%d",&no);
	for(i=1;i<=no;i++)
	{
		aa=pow(2,i);
		if(x>n)
		{
			d=i;
			break;
		}
	}
	y=pow(2,d);
	printf("\n%d",y);
	
	return 0;
}
